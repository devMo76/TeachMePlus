export function isAdmin(role: string | null): boolean {
  // TODO: Check is_admin flag from profiles table.
  return false
}

export function isTutor(role: string | null): boolean {
  // TODO: Check approved tutor_application exists for user.
  return false
}

export function isStudent(role: string | null): boolean {
  // TODO: Check user profile exists as student.
  return false
}
